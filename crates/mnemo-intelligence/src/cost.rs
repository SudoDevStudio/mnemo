/// Cost tracker stub.
/// Will maintain cost log and update eviction priority scores
/// across L1/L2.
#[derive(Default)]
pub struct CostTracker;

impl CostTracker {
    pub fn new() -> Self {
        Self
    }
}
